{
  "name": "sst-monorepo-vite-react-hono-sample",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.2"
  },
  "packageManager": "npm@11.3.0",
  "engines": {
    "node": ">=23"
  },
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ]
}
